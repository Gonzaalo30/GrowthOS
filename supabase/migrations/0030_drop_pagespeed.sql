-- El panel de "Velocidad real (Google PageSpeed Insights)" del Sprint 4.20 se
-- retira del producto (decisión del fundador) — nunca llegó a configurarse la
-- API key real, así que la tabla nunca tuvo datos de producción.
drop table if exists public.pagespeed_snapshots;
